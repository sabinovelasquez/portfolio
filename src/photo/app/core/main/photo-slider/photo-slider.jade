.cover
  .info
    .name {{slider.name}}
  .actions
    .enter
      | ver caso
    .explore
      | ver más en {{slider.category}}
  img.main.img-responsive.center-block(ng-src='{{slider.coverUrl}}')